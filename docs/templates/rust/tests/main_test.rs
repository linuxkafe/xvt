#[cfg(test)]
mod tests {
    #[test]
    fn test_main_runs() {
        // Smoke test - just verify the binary compiles
    }

    #[test]
    fn test_basic_math() {
        assert_eq!(1 + 1, 2);
    }
}